shortnick.github.io buildnotes

xxshould narrow the left column and widen the center, too much wasted whitespace

list-style:none for Education, Accomplishments

Capabilities: space between Programming-GIS as there is above Language&Writing 

xxcheck underlines for topic headers

xxchange the font for somethings?

adjust "hi i'm nick" and the link bar so they've got even bases

add up-arrow (to top) links to each linked section header

relate/schematize the size/bolding for main heads and major elements under them
- subject heads should be colored, large, bold, underlined? with lots of top margin, and a minor undermargin. 
- sub heads should be bold-ish, maybe black?, minor top/bottom margin
- darken text color a few shades from template default, maybe leave the header text as-is 

header: 
	- add an image that spans top
	- float the header on the left still
	- move the navigation row on top of the image

add buttons for github account, the locations, and highlight/reveal? for write and call

anchor the "hosted on github pages - theme by orderedlist" so it doesn't float over the header

unify color pallete
Conservative blue pallete colors:
/* Coolors Exported Palette - coolors.co/ffffff-00171f-003459-007ea7-00a8e8 */

the pocket archive list elements are specified at h4.feed-item-title a

p.view class specification for links?